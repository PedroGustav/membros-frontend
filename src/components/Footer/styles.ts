import styled from "styled-components";

export const Container = styled.footer`
    margin-top: 1rem;
    margin-bottom: 5rem;
    padding: 2rem;
    font-family: 'Open Sans';
    
    p{
        text-align: center;
        margin: 0 auto;
        
        b{
            color: var(--primary);
        }
    }
`;